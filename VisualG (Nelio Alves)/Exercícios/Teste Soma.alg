algoritmo "Para

var

x, i, soma: inteiro

inicio

  escreva("Quantos números serão digitados? ")
  leia(x)

  soma <- 0

  para i de 1 ate x faca
    escreva("Digite um número: ")
    leia(x)
    soma <- soma + x
  fimpara
  
  escreva(soma)


fimalgoritmo
